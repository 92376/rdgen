name: Custom Android Client Generator (Windows)
run-name: Custom Android Client Generator (Windows)
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'version to buld'
        required: true
        default: ''
        type: string
      source_repository:
        description: 'owner/repository containing the DIY RustDesk source'
        required: true
        default: '92376/rustdesk-diy'
        type: string
      source_ref:
        description: 'branch, tag, or commit to build from the DIY source repository'
        required: true
        default: '1.4.9'
        type: string
      android_arch:
        description: 'Android CPU architecture'
        required: true
        default: 'aarch64'
        type: choice
        options:
          - aarch64
          - armv7
          - x86_64
      zip_url:
        description: 'url to zip of json'
        required: true
        default: ''
        type: string


env:
  SCITER_RUST_VERSION: "1.75" # https://github.com/rustdesk/rustdesk/discussions/7503, also 1.78 has ABI change which causes our sciter version not working, https://blog.rust-lang.org/2024/03/30/i128-layout-update.html
  RUST_VERSION: "1.75" # sciter failed on m1 with 1.78 because of https://blog.rust-lang.org/2024/03/30/i128-layout-update.html
  CARGO_NDK_VERSION: "3.5.4"
  SCITER_ARMV7_CMAKE_VERSION: "3.29.7"
  SCITER_NASM_DEBVERSION: "2.14-1"
  LLVM_VERSION: "15.0.6"
  FLUTTER_VERSION: "3.24.5"
  ANDROID_FLUTTER_VERSION: "3.24.5"
  # for arm64 linux because official Dart SDK does not work
  FLUTTER_ELINUX_VERSION: "3.16.9"
  VCPKG_BINARY_SOURCES: "clear;x-gha,readwrite"
  # vcpkg version: 2024.07.12
  VCPKG_COMMIT_ID: "120deac3062162151622ca4860575a33844ba10b"
  ARMV7_VCPKG_COMMIT_ID: "6f29f12e82a8293156836ad81cc9bf5af41fe836"
  VERSION: "${{ inputs.version }}"
  NDK_VERSION: "r28c"
  ANDROID_BUILD_TOOLS_VERSION: "30.0.3"
  # Optional release signing. When the key is absent, the APK remains debug-signed.
  ANDROID_SIGNING_KEY: "${{ secrets.ANDROID_SIGNING_KEY }}"
  ANDROID_ALIAS: "${{ secrets.ANDROID_ALIAS }}"
  ANDROID_KEY_STORE_PASSWORD: "${{ secrets.ANDROID_KEY_STORE_PASSWORD }}"
  ANDROID_KEY_PASSWORD: "${{ secrets.ANDROID_KEY_PASSWORD }}"
  UPLOAD_ARTIFACT: 'true'
  STATUS_URL: "${{ secrets.GENURL }}/updategh"

jobs:
  setup:
    uses: ./.github/workflows/fetch-encrypted-secrets.yml
    with:
      zip_url_json: ${{ inputs.zip_url }}

  generate-bridge-linux:
    uses: ./.github/workflows/bridge.yml
    with:
      version: ${{ inputs.version }}
      source_repository: ${{ inputs.source_repository }}
      source_ref: ${{ inputs.source_ref }}

  build-rustdesk-android:
    needs: [generate-bridge-linux, setup]
    name: build rustdesk android apk ${{ matrix.job.target }}
    runs-on: [self-hosted, Windows, X64, rustdesk-android]
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        job: ${{ fromJSON(inputs.android_arch == 'armv7' && '[{"arch":"armv7","target":"armv7-linux-androideabi","os":"ubuntu-24.04","reltype":"release","suffix":""}]' || inputs.android_arch == 'x86_64' && '[{"arch":"x86_64","target":"x86_64-linux-android","os":"ubuntu-24.04","reltype":"release","suffix":""}]' || '[{"arch":"aarch64","target":"aarch64-linux-android","os":"ubuntu-24.04","reltype":"release","suffix":""}]') }}
        # Keep the job object shape below in the expression above in sync with
        # the target-specific build cases used later in this workflow.
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Configure and validate Windows Android build tools
        shell: pwsh
        run: |
          function Get-RunnerVariable([string] $name, [bool] $required = $true) {
            $value = [Environment]::GetEnvironmentVariable($name, 'Process')
            if (-not $value) { $value = [Environment]::GetEnvironmentVariable($name, 'Machine') }
            if (-not $value) { $value = [Environment]::GetEnvironmentVariable($name, 'User') }
            if ($required -and -not $value) { throw "Required runner environment variable $name is not configured" }
            return $value
          }

          $androidVcpkgRoot = Get-RunnerVariable 'RUSTDESK_ANDROID_VCPKG_ROOT' $false
          if (-not $androidVcpkgRoot) { $androidVcpkgRoot = Get-RunnerVariable 'VCPKG_ROOT' }

          $values = @{
            VCPKG_ROOT = $androidVcpkgRoot
            LIBCLANG_PATH = Get-RunnerVariable 'LIBCLANG_PATH'
            ANDROID_HOME = Get-RunnerVariable 'ANDROID_HOME'
            ANDROID_NDK_HOME = Get-RunnerVariable 'ANDROID_NDK_HOME'
            JAVA_HOME = Get-RunnerVariable 'JAVA_HOME'
            RUSTDESK_FLUTTER_HOME = Get-RunnerVariable 'RUSTDESK_FLUTTER_HOME'
            CARGO_HOME = Get-RunnerVariable 'CARGO_HOME'
            RUSTUP_HOME = Get-RunnerVariable 'RUSTUP_HOME'
          }
          foreach ($entry in $values.GetEnumerator()) {
            if (-not (Test-Path -LiteralPath $entry.Value)) {
              throw "$($entry.Key) points to a missing path: $($entry.Value)"
            }
          }

          $pythonHome = Get-RunnerVariable 'RUSTDESK_PYTHON_HOME' $false
          if (-not $pythonHome) { $pythonHome = 'D:\develop\python3.13' }
          $pythonExe = Join-Path $pythonHome 'python.exe'
          $gitBash = 'C:\Program Files\Git\bin\bash.exe'
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          if (-not (Test-Path -LiteralPath $vswhere)) { throw "Visual Studio locator is missing: $vswhere" }
          $vsPath = & $vswhere -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath
          if (-not $vsPath) { throw 'Visual Studio 2022 C++ build tools are not installed' }
          $vcvars = Join-Path $vsPath 'VC\Auxiliary\Build\vcvars64.bat'
          if (-not (Test-Path -LiteralPath $vcvars)) { throw "vcvars64.bat is missing: $vcvars" }

          $vsEnvironment = cmd.exe /s /c "`"$vcvars`" >nul && set"
          foreach ($line in $vsEnvironment) {
            $index = $line.IndexOf('=')
            if ($index -gt 0) {
              [Environment]::SetEnvironmentVariable($line.Substring(0, $index), $line.Substring($index + 1), 'Process')
            }
          }
          foreach ($entry in $values.GetEnumerator()) {
            Set-Item -Path "Env:$($entry.Key)" -Value $entry.Value
          }

          $linker = Join-Path $env:VCToolsInstallDir 'bin\Hostx64\x64\link.exe'
          $rustupExe = Join-Path $values.CARGO_HOME 'bin\rustup.exe'
          if (-not (Test-Path -LiteralPath $rustupExe)) {
            $rustupBootstrap = Get-RunnerVariable 'RUSTDESK_RUSTUP_BOOTSTRAP' $false
            if (-not $rustupBootstrap) {
              $rustupBootstrap = Join-Path ([Environment]::GetFolderPath('UserProfile')) '.cargo\bin\rustup.exe'
            }
            if (-not (Test-Path -LiteralPath $rustupBootstrap)) {
              throw "rustup.exe is missing and no bootstrap executable is available: $rustupBootstrap"
            }
            Copy-Item -LiteralPath $rustupBootstrap -Destination $rustupExe
            Write-Host "Restored rustup.exe from the runner bootstrap copy."
          }
          $requiredPaths = @(
            (Join-Path $values.VCPKG_ROOT 'vcpkg.exe'),
            (Join-Path $values.LIBCLANG_PATH 'libclang.dll'),
            (Join-Path $values.CARGO_HOME 'bin\cargo.exe'),
            $rustupExe,
            (Join-Path $values.RUSTDESK_FLUTTER_HOME 'bin\flutter.bat'),
            (Join-Path $values.ANDROID_NDK_HOME 'toolchains\llvm\prebuilt\windows-x86_64'),
            (Join-Path $values.ANDROID_HOME "build-tools\$env:ANDROID_BUILD_TOOLS_VERSION\apksigner.bat"),
            (Join-Path $values.ANDROID_HOME 'platforms\android-31'),
            (Join-Path $values.ANDROID_HOME 'platforms\android-32'),
            (Join-Path $values.ANDROID_HOME 'platforms\android-33'),
            (Join-Path $values.JAVA_HOME 'bin\java.exe'),
            $pythonExe,
            $gitBash,
            $linker
          )
          foreach ($requiredPath in $requiredPaths) {
            if (-not (Test-Path -LiteralPath $requiredPath)) {
              throw "Required Windows Android build tool is missing: $requiredPath"
            }
          }

          $javaVersion = (& (Join-Path $values.JAVA_HOME 'bin\java.exe') -version 2>&1 | Out-String)
          if ($javaVersion -notmatch 'version "17\.') { throw "JDK 17 is required; detected: $javaVersion" }

          $driveName = ([IO.Path]::GetPathRoot($env:RUNNER_WORKSPACE)).TrimEnd('\').TrimEnd(':')
          $freeSpace = (Get-PSDrive -Name $driveName).Free
          if ($freeSpace -lt 100GB) {
            throw "At least 100 GB free disk space is required; available: $([math]::Round($freeSpace / 1GB, 1)) GB"
          }

          $persisted = @{
            VCPKG_ROOT = $values.VCPKG_ROOT
            RUSTDESK_ANDROID_VCPKG_ROOT = $values.VCPKG_ROOT
            LIBCLANG_PATH = $values.LIBCLANG_PATH
            ANDROID_HOME = $values.ANDROID_HOME
            ANDROID_NDK_HOME = $values.ANDROID_NDK_HOME
            ANDROID_NDK_ROOT = $values.ANDROID_NDK_HOME
            NDK_HOME = $values.ANDROID_NDK_HOME
            JAVA_HOME = $values.JAVA_HOME
            RUSTDESK_FLUTTER_HOME = $values.RUSTDESK_FLUTTER_HOME
            CARGO_HOME = $values.CARGO_HOME
            RUSTUP_HOME = $values.RUSTUP_HOME
            RUSTDESK_PYTHON_HOME = $pythonHome
            CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_LINKER = $linker
          }
          foreach ($name in @('INCLUDE', 'LIB', 'LIBPATH', 'VCINSTALLDIR', 'VCToolsInstallDir', 'WindowsSdkDir', 'WindowsSDKVersion', 'UCRTVersion', 'UniversalCRTSdkDir')) {
            $value = [Environment]::GetEnvironmentVariable($name, 'Process')
            if ($value) { $persisted[$name] = $value }
          }
          foreach ($entry in $persisted.GetEnumerator()) {
            "$($entry.Key)=$($entry.Value)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          }
          foreach ($pathEntry in $env:Path.Split(';', [StringSplitOptions]::RemoveEmptyEntries)) {
            $pathEntry | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }
          foreach ($pathEntry in @((Join-Path $values.CARGO_HOME 'bin'), (Join-Path $values.RUSTDESK_FLUTTER_HOME 'bin'), $values.VCPKG_ROOT, $values.LIBCLANG_PATH, $pythonHome, (Join-Path $pythonHome 'Scripts'), (Split-Path -Parent $gitBash))) {
            $pathEntry | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }

          & $rustupExe toolchain install $env:RUST_VERSION --profile minimal --component rustfmt
          & $rustupExe target add --toolchain $env:RUST_VERSION ${{ matrix.job.target }}
          & (Join-Path $values.RUSTDESK_FLUTTER_HOME 'bin\flutter.bat') config --jdk-dir $values.JAVA_HOME
          $flutterVersion = & (Join-Path $values.RUSTDESK_FLUTTER_HOME 'bin\flutter.bat') --version
          if (($flutterVersion | Out-String) -notmatch [regex]::Escape($env:ANDROID_FLUTTER_VERSION)) {
            throw "Expected Flutter $env:ANDROID_FLUTTER_VERSION under RUSTDESK_FLUTTER_HOME"
          }
          & (Join-Path $values.VCPKG_ROOT 'vcpkg.exe') version
          & $pythonExe --version
      - uses: actions/download-artifact@v4
        with:
          name: encrypted-secrets-zip

      - name: Load Secrets
        uses: ./.github/actions/decrypt-secrets
        with:
          zip_password: ${{ secrets.ZIP_PASSWORD }}

      - name: Finalize and Cleanup zip/json
        if: always() # Run even if previous steps fail
        continue-on-error: true
        uses: fjogeleit/http-request-action@v1
        with:
          url: "${{ secrets.GENURL }}/cleanzip"
          method: 'POST'
          customHeaders: '{"Content-Type": "application/json"}'
          data: '{"uuid": "${{ env.uuid }}"}'

      - name: Export GitHub Actions cache environment variables
        uses: actions/github-script@v7
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Set rdgen value
        if: ${{ env.rdgen == 'true' }}
        shell: pwsh
        run: |
          "STATUS_URL=${{ secrets.GENURL }}/updategh" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Set rdgen value
        if: ${{ env.rdgen == 'false' }}
        shell: pwsh
        run: |
          "STATUS_URL=${{ env.apiServer }}/api/updategh" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Restrict source repositories on the self-hosted runner
        shell: pwsh
        env:
          SOURCE_REPOSITORY: ${{ inputs.source_repository }}
        run: |
          $trustedRepositories = @('rustdesk/rustdesk', '92376/rustdesk-diy')
          if ($env:SOURCE_REPOSITORY -notin $trustedRepositories) {
            throw "Self-hosted Windows builds only accept trusted source repositories."
          }

      - name: Checkout DIY source code
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.source_repository }}
          ref: ${{ inputs.source_ref }}
          submodules: recursive

      - name: Install vcpkg dependencies
        run: |
          case ${{ matrix.job.target }} in
            aarch64-linux-android)
              ANDROID_TARGET=arm64-v8a
            ;;
            armv7-linux-androideabi)
              ANDROID_TARGET=armeabi-v7a
            ;;
            x86_64-linux-android)
              ANDROID_TARGET=x86_64
            ;;
            i686-linux-android)
              ANDROID_TARGET=x86
            ;;
          esac
          if ! ./flutter/build_android_deps.sh "${ANDROID_TARGET}"; then
            find "${VCPKG_ROOT}/" -name "*.log" | while read -r _1; do
              echo "$_1:"
              echo "======"
              cat "$_1"
              echo "======"
              echo ""
            done
            exit 1
          fi
        shell: bash

      - name: Restore bridge files
        uses: actions/download-artifact@master
        with:
          name: bridge-artifact
          path: ./

      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: rustdesk-lib-cache-android # TODO: drop '-android' part after caches are invalidated
          key: ${{ matrix.job.target }}
          # This runner uses a persistent shared CARGO_HOME. The action's default
          # post-step cleanup removes binaries that predate the build, including
          # rustup.exe, and breaks the next Android or Windows job.
          cache-bin: false

      ###########################################################echo "${{ env.iconbase64 }}" | base64 -d > ./res/icon.png
      - name: Prepare custom Android icon on Windows
        if: ${{ env.iconlink_url != 'false' }}
        shell: bash
        run: |
          curl.exe --fail --location --retry 2 --output ./res/icon.png "${{ env.iconlink_url }}/get_png?filename=${{ env.iconlink_file }}&uuid=${{ env.iconlink_uuid }}"
          sed -i '/android: true/a \ \ adaptive_icon_background: "#ffffff"' ./flutter/pubspec.yaml
          sed -i '/adaptive_icon_background/a \ \ adaptive_icon_foreground: "../res/icon.png"' ./flutter/pubspec.yaml
          sed -i '/adaptive_icon_foreground:/a \ \ adaptive_icon_foreground_inset: 32' ./flutter/pubspec.yaml
          sed -i '/ic_launcher_background/d' ./flutter/android/app/src/main/res/values/colors.xml

      - name: set server, key, and apiserver
        continue-on-error: true
        shell: bash
        run: |
          sed -i -e 's|rs-ny.rustdesk.com|${{ env.server }}|' ./libs/hbb_common/src/config.rs
          sed -i -e 's|OeVuKk5nlHiXp+APNn0Y3pC1Iwpwn44JGqrQCsWqmBw=|${{ env.key }}|' ./libs/hbb_common/src/config.rs
          sed -i -e 's|https://admin.rustdesk.com|${{ env.apiServer }}|' ./src/common.rs
          # ./flutter/pubspec.yaml
          #sed -i '/intl:/a \ \ archive: ^3.6.1' ./flutter/pubspec.yaml

      - name: change appname to custom
        if: env.appname != 'rustdesk'
        continue-on-error: true
        shell: bash
        run: |
          sed -i -e 's|description = "RustDesk Remote Desktop"|description = "${{ env.appname }}"|' ./Cargo.toml
          sed -i -e 's|ProductName = "RustDesk"|ProductName = "${{ env.appname }}"|' ./Cargo.toml
          sed -i -e 's|FileDescription = "RustDesk Remote Desktop"|FileDescription = "${{ env.appname }}"|' ./Cargo.toml
          sed -i -e 's|OriginalFilename = "rustdesk.exe"|OriginalFilename = "${{ env.appname }}.exe"|' ./Cargo.toml
          sed -i 's|name = "RustDesk"|name = "${{ env.appname }}"|' ./Cargo.toml
          sed -i -e 's|description = "RustDesk Remote Desktop"|description = "${{ env.appname }}"|' ./libs/portable/Cargo.toml
          sed -i -e 's|ProductName = "RustDesk"|ProductName = "${{ env.appname }}"|' ./libs/portable/Cargo.toml
          sed -i -e 's|FileDescription = "RustDesk Remote Desktop"|FileDescription = "${{ env.appname }}"|' ./libs/portable/Cargo.toml
          sed -i -e 's|OriginalFilename = "rustdesk.exe"|OriginalFilename = "${{ env.appname }}.exe"|' ./libs/portable/Cargo.toml
          sed -i -e 's|const APP_PREFIX: \&str = "rustdesk";|const APP_PREFIX: \&str = "${{ env.appname }}";|' ./libs/portable/src/main.rs
          find ./src/lang -name "*.rs" -exec sed -i -e 's|RustDesk|${{ env.appname }}|' {} \;
          sed -i -e 's|RustDesk|${{ env.appname }}|' ./flutter/android/app/src/main/res/values/strings.xml
          sed -i -e "s|title: 'RustDesk'|title: '${{ env.appname }}'|" ./flutter/lib/main.dart
          sed -i -e "s|return 'RustDesk';|return '${{ env.appname }}';|" ./flutter/lib/web/bridge.dart
          sed -i 's|android:label="RustDesk"|android:label="${{ env.appname }}"|' ./flutter/android/app/src/main/AndroidManifest.xml
          sed -i 's|android:label="RustDesk Input"|android:label="${{ env.appname }} Input"|' ./flutter/android/app/src/main/AndroidManifest.xml
          sed -i 's|RustDesk is Open|${{ env.appname }} is Open|' ./flutter/android/app/src/main/kotlin/com/carriez/flutter_hbb/BootReceiver.kt
          sed -i 's|Show Rustdesk|Show ${{ env.appname }}|' ./flutter/android/app/src/main/kotlin/com/carriez/flutter_hbb/FloatingWindowService.kt
          sed -i 's|"RustDesk"|"${{ env.appname }}"|' ./flutter/android/app/src/main/kotlin/com/carriez/flutter_hbb/MainService.kt
          sed -i 's|"RustDesk Service|"${{ env.appname }} Service|' ./flutter/android/app/src/main/kotlin/com/carriez/flutter_hbb/MainService.kt
          sed -i 's|RustDesk|${{ env.appname }}|' ./flutter/lib/main.dart
          sed -i 's|"RustDesk"|"${{ env.appname }}"|' ./flutter/lib/desktop/widgets/tabbar_widget.dart
          sed -i 's|"RustDesk"|"${{ env.appname }}"|' ./libs/hbb_common/src/config.rs
          sed -i -e 's|Purslane Tech Pte. Ltd.|${{ env.compname }}|' ./flutter/lib/desktop/pages/desktop_setting_page.dart
          sed -i -e 's|Purslane Tech Pte. Ltd.|${{ env.compname }}|' ./Cargo.toml
          sed -i -e 's|Purslane Tech Pte. Ltd.|${{ env.compname }}|' ./libs/portable/Cargo.toml
          sed -i -e 's|Purslane Tech Pte. Ltd.|${{ env.compname }}|' ./src/main.rs
          sed -i -e 's|Purslane Ltd.|${{ env.compname }}|' ./flutter/lib/desktop/pages/desktop_setting_page.dart
          sed -i -e 's|Purslane Ltd.|${{ env.compname }}|' ./Cargo.toml
          sed -i -e 's|Purslane Ltd.|${{ env.compname }}|' ./libs/portable/Cargo.toml
          sed -i -e 's|Purslane Ltd|${{ env.compname }}|' ./src/main.rs

      - name: change url to custom
        if: env.urlLink != 'https://rustdesk.com'
        continue-on-error: true
        shell: bash
        run: |
          sed -i -e 's|Homepage: https://rustdesk.com|Homepage: ${{ env.urlLink }}|' ./build.py
          sed -i -e "s|launchUrl(Uri.parse('https://rustdesk.com'));|launchUrl(Uri.parse('${{ env.urlLink }}'));|" ./flutter/lib/common.dart
          sed -i -e "s|launchUrlString('https://rustdesk.com');|launchUrlString('${{ env.urlLink }}');|" ./flutter/lib/desktop/pages/desktop_setting_page.dart
          sed -i -e "s|launchUrlString('https://rustdesk.com/privacy.html')|launchUrlString('${{ env.urlLink }}/privacy.html')|" ./flutter/lib/desktop/pages/desktop_setting_page.dart
          sed -i -e "s|const url = 'https://rustdesk.com/';|const url = '${{ env.urlLink }}';|" ./flutter/lib/mobile/pages/settings_page.dart
          sed -i -e "s|launchUrlString('https://rustdesk.com/privacy.html')|launchUrlString('${{ env.urlLink }}/privacy.html')|" ./flutter/lib/mobile/pages/settings_page.dart
          sed -i -e "s|child: Text('rustdesk.com',|child: Text('${{ env.urlLink }}',|" ./flutter/lib/mobile/pages/settings_page.dart
          sed -i -e "s|https://rustdesk.com/privacy.html|${{ env.urlLink }}/privacy.html|" ./flutter/lib/desktop/pages/install_page.dart

      - name: change app id to custom
        if: env.androidappid != 'com.carriez.flutter_hbb'
        continue-on-error: true
        shell: bash
        run: |
          sed -i -e 's|com.carriez.flutter_hbb|${{ env.androidappid }}|' ./flutter/android/app/build.gradle

      - name: change download link to custom
        if: env.downloadLink != 'https://rustdesk.com/download'
        continue-on-error: true
        shell: bash
        run: |
          sed -i -e 's|https://rustdesk.com/download|${{ env.downloadLink }}|' ./flutter/lib/desktop/pages/desktop_home_page.dart
          sed -i -e 's|https://rustdesk.com/download|${{ env.downloadLink }}|' ./flutter/lib/mobile/pages/connection_page.dart
          sed -i -e 's|https://rustdesk.com/download|${{ env.downloadLink }}|' ./src/ui/index.tis

      - name: allow custom.txt
        shell: bash
        run: |
          sed -i -e '/const KEY:/,/};/d' ./src/common.rs
          sed -i -e '/let Ok(data) = sign::verify(&data, &pk)/,/};/d' ./src/common.rs
          # sed -i '/intl:/a \ \ archive: ^3.6.1' ./flutter/pubspec.yaml

      - name: Embed custom client config in Android entry points
        shell: bash
        env:
          CUSTOM_CLIENT_CONFIG: ${{ env.custom }}
        run: |
          python - <<'PY'
          import os
          from pathlib import Path

          custom = os.environ["CUSTOM_CLIENT_CONFIG"]
          if not custom or any(ch not in "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=" for ch in custom):
              raise SystemExit("CUSTOM_CLIENT_CONFIG is empty or is not base64")

          dart_path = Path("flutter/lib/models/native_model.dart")
          dart = dart_path.read_text(encoding="utf-8")
          dart_old = "customClientConfig: '',"
          dart_new = f"customClientConfig: '{custom}',"
          if dart.count(dart_old) != 1:
              raise SystemExit(f"Expected one Android/Dart custom config placeholder, found {dart.count(dart_old)}")
          dart_path.write_text(dart.replace(dart_old, dart_new), encoding="utf-8")

          service_path = Path("flutter/android/app/src/main/kotlin/com/carriez/flutter_hbb/MainService.kt")
          service = service_path.read_text(encoding="utf-8")
          service_old = 'FFI.startServer(configPath, "")'
          service_new = f'FFI.startServer(configPath, "{custom}")'
          if service.count(service_old) != 1:
              raise SystemExit(f"Expected one Android service custom config placeholder, found {service.count(service_old)}")
          service_path.write_text(service.replace(service_old, service_new), encoding="utf-8")
          PY

      - name: Remove scam alert on Android
        shell: bash
        run: |
          sed -i 's/bind.mainGetLocalOption(key:\s*"show-scam-warning")/"N"/g' ./flutter/lib/mobile/pages/server_page.dart

      - name: fix connection delay
        continue-on-error: true
        if: ${{ env.delayFix == 'true' }}
        shell: bash
        run: |
          sed -i -e 's|!key.is_empty()|false|' ./src/client.rs

      - name: use X for offline display instead of orange circle
        if: env.xOffline == 'true'
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 5
          max_attempts: 3
          retry_wait_seconds: 15
          shell: bash
          continue_on_error: true
          command: |
            wget https://raw.githubusercontent.com/bryangerlach/rdgen/refs/heads/master/.github/patches/xoffline.diff
            git apply xoffline.diff

      - name: hide-cm
        if: env.hidecm == 'true'
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 5
          max_attempts: 3
          retry_wait_seconds: 15
          shell: bash
          continue_on_error: true
          command: |
            wget https://raw.githubusercontent.com/bryangerlach/rdgen/refs/heads/master/.github/patches/hidecm.diff
            git apply hidecm.diff

      - name: removeNewVersionNotif
        continue-on-error: true
        if: env.removeNewVersionNotif == 'true'
        shell: bash
        run: |
          sed -i -e 's|updateUrl.isNotEmpty|false|' ./flutter/lib/desktop/pages/desktop_home_page.dart
          sed -i '/let (request, url) =/,/Ok(())/{/Ok(())/!d}' ./src/common.rs

      - name: replace flutter icons
        if: ${{ env.iconlink_url != 'false' }}
        shell: bash
        run: |
          pushd ./flutter
          flutter pub get
          dart run flutter_launcher_icons
          popd
          sed -i '/ic_launcher_background/d' ./flutter/android/app/src/main/res/values/colors.xml
      ##########################################################

      - name: Build rustdesk lib
        env:
          ANDROID_NDK_HOME: ${{ env.ANDROID_NDK_HOME }}
          ANDROID_NDK_ROOT: ${{ env.ANDROID_NDK_HOME }}
        shell: bash
        run: |
          rustup target add ${{ matrix.job.target }}
          cargo install cargo-ndk --version ${{ env.CARGO_NDK_VERSION }} --locked
          case ${{ matrix.job.target }} in
            aarch64-linux-android)
            ./flutter/ndk_arm64.sh
            mkdir -p ./flutter/android/app/src/main/jniLibs/arm64-v8a
            cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/arm64-v8a/librustdesk.so
            ;;
            armv7-linux-androideabi)
              ./flutter/ndk_arm.sh
              mkdir -p ./flutter/android/app/src/main/jniLibs/armeabi-v7a
              cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/armeabi-v7a/librustdesk.so
            ;;
            x86_64-linux-android)
              ./flutter/ndk_x64.sh
              mkdir -p ./flutter/android/app/src/main/jniLibs/x86_64
              cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/x86_64/librustdesk.so
            ;;
            i686-linux-android)
              ./flutter/ndk_x86.sh
              mkdir -p ./flutter/android/app/src/main/jniLibs/x86
              cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/x86/librustdesk.so
            ;;
          esac

      - name: Upload Rustdesk library to Artifacts
        uses: actions/upload-artifact@master
        with:
          name: librustdesk.so.${{ matrix.job.target }}
          path: ./target/${{ matrix.job.target }}/release/liblibrustdesk.so

      - name: Build rustdesk
        shell: bash
        run: |
          NDK_HOST=windows-x86_64
          # Increase Gradle JVM memory for CI builds
          sed -i "s/org.gradle.jvmargs=-Xmx1024M/org.gradle.jvmargs=-Xmx2g/g" ./flutter/android/gradle.properties
          # The DIY source uses Android debug signing for installable, secret-free release APKs.
          case ${{ matrix.job.target }} in
            aarch64-linux-android)
            mkdir -p ./flutter/android/app/src/main/jniLibs/arm64-v8a
            cp "${ANDROID_NDK_HOME}/toolchains/llvm/prebuilt/${NDK_HOST}/sysroot/usr/lib/aarch64-linux-android/libc++_shared.so" ./flutter/android/app/src/main/jniLibs/arm64-v8a/
            cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/arm64-v8a/librustdesk.so
            # build flutter
            pushd flutter
            flutter build apk "--${{ matrix.job.reltype }}" --target-platform android-arm64 --split-per-abi
            mv build/app/outputs/flutter-apk/app-arm64-v8a-${{ matrix.job.reltype }}.apk ../rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}${{ matrix.job.suffix }}.apk
            ;;
            armv7-linux-androideabi)
              mkdir -p ./flutter/android/app/src/main/jniLibs/armeabi-v7a
              cp "${ANDROID_NDK_HOME}/toolchains/llvm/prebuilt/${NDK_HOST}/sysroot/usr/lib/arm-linux-androideabi/libc++_shared.so" ./flutter/android/app/src/main/jniLibs/armeabi-v7a/
              cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/armeabi-v7a/librustdesk.so
              # build flutter
              pushd flutter
              flutter build apk "--${{ matrix.job.reltype }}" --target-platform android-arm --split-per-abi
              mv build/app/outputs/flutter-apk/app-armeabi-v7a-${{ matrix.job.reltype }}.apk ../rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}${{ matrix.job.suffix }}.apk
            ;;
            x86_64-linux-android)
              mkdir -p ./flutter/android/app/src/main/jniLibs/x86_64
              cp "${ANDROID_NDK_HOME}/toolchains/llvm/prebuilt/${NDK_HOST}/sysroot/usr/lib/x86_64-linux-android/libc++_shared.so" ./flutter/android/app/src/main/jniLibs/x86_64/
              cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/x86_64/librustdesk.so
              # build flutter
              pushd flutter
              flutter build apk "--${{ matrix.job.reltype }}" --target-platform android-x64 --split-per-abi
              mv build/app/outputs/flutter-apk/app-x86_64-${{ matrix.job.reltype }}.apk ../rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}${{ matrix.job.suffix }}.apk
            ;;
            i686-linux-android)
              mkdir -p ./flutter/android/app/src/main/jniLibs/x86
              cp "${ANDROID_NDK_HOME}/toolchains/llvm/prebuilt/${NDK_HOST}/sysroot/usr/lib/i686-linux-android/libc++_shared.so" ./flutter/android/app/src/main/jniLibs/x86/
              cp ./target/${{ matrix.job.target }}/release/liblibrustdesk.so ./flutter/android/app/src/main/jniLibs/x86/librustdesk.so
              # build flutter
              pushd flutter
              flutter build apk "--${{ matrix.job.reltype }}" --target-platform android-x86 --split-per-abi
              mv build/app/outputs/flutter-apk/app-x86-${{ matrix.job.reltype }}.apk ../rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}${{ matrix.job.suffix }}.apk
            ;;
          esac
          popd
          mkdir -p signed-apk; pushd signed-apk
          mv ../rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}${{ matrix.job.suffix }}.apk ./${{ env.filename }}-${{ matrix.job.arch }}.apk
          popd

      - name: Sign app APK with release key
        if: ${{ env.ANDROID_SIGNING_KEY != '' && env.ANDROID_ALIAS != '' && env.ANDROID_KEY_STORE_PASSWORD != '' && env.ANDROID_KEY_PASSWORD != '' }}
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: ./signed-apk
          signingKeyBase64: ${{ secrets.ANDROID_SIGNING_KEY }}
          alias: ${{ secrets.ANDROID_ALIAS }}
          keyStorePassword: ${{ secrets.ANDROID_KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: ${{ env.ANDROID_BUILD_TOOLS_VERSION }}

      - name: Validate release signing configuration
        if: ${{ (env.ANDROID_SIGNING_KEY != '' || env.ANDROID_ALIAS != '' || env.ANDROID_KEY_STORE_PASSWORD != '' || env.ANDROID_KEY_PASSWORD != '') && !(env.ANDROID_SIGNING_KEY != '' && env.ANDROID_ALIAS != '' && env.ANDROID_KEY_STORE_PASSWORD != '' && env.ANDROID_KEY_PASSWORD != '') }}
        shell: pwsh
        run: throw 'Release signing configuration is incomplete; set all four ANDROID_* signing secrets or leave all of them empty.'

      - name: Verify signed APK
        shell: pwsh
        run: |
          $apksigner = Join-Path $env:ANDROID_HOME "build-tools\$env:ANDROID_BUILD_TOOLS_VERSION\apksigner.bat"
          $apks = @(Get-ChildItem -LiteralPath './signed-apk' -Filter '*.apk')
          if ($apks.Count -eq 0) { throw 'No APK was produced for signature verification' }
          $apks | ForEach-Object {
            & $apksigner verify --verbose $_.FullName
            if ($LASTEXITCODE -ne 0) { throw "APK signature verification failed: $($_.FullName)" }
          }
      - name: send file to rdgen server
        if: ${{ env.rdgen == 'true' }}
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 5
          max_attempts: 3
          retry_wait_seconds: 15
          shell: bash
          command: |
            curl -i -X POST -H "Content-Type: multipart/form-data" -H "Authorization: Bearer ${{ env.token }}" -F "file=@./signed-apk/${{ env.filename }}-${{ matrix.job.arch }}.apk" -F "uuid=${{ env.uuid }}" ${{ secrets.GENURL }}/save_custom_client

      - name: send file to api server
        if: ${{ env.rdgen == 'false' }}
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 5
          max_attempts: 3
          retry_wait_seconds: 15
          shell: bash
          command: |
            curl -i -X POST -H "Content-Type: multipart/form-data" -H "Authorization: Bearer ${{ env.token }}" -F "file=@./signed-apk/${{ env.filename }}-${{ matrix.job.arch }}.apk" ${{ env.apiServer }}/api/save_custom_client

  deploy:
    needs: [build-rustdesk-android]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: encrypted-secrets-zip

      - name: Load Secrets
        uses: ./.github/actions/decrypt-secrets
        with:
          zip_password: ${{ secrets.ZIP_PASSWORD }}

      - name: Finalize and Cleanup zip/json
        if: always() # Run even if previous steps fail
        continue-on-error: true
        uses: fjogeleit/http-request-action@v1
        with:
          url: "${{ secrets.GENURL }}/cleanzip"
          method: 'POST'
          customHeaders: '{"Content-Type": "application/json"}'
          data: '{"uuid": "${{ env.uuid }}"}'

      - name: Set rdgen value
        if: ${{ env.rdgen == 'true' }}
        run: |
          echo "STATUS_URL=${{ secrets.GENURL }}/updategh" >> $GITHUB_ENV

      - name: Set rdgen value
        if: ${{ env.rdgen == 'false' }}
        run: |
          echo "STATUS_URL=${{ env.apiServer }}/api/updategh" >> $GITHUB_ENV

      - uses: geekyeggo/delete-artifact@v5
        continue-on-error: true
        with:
            name: ${{ env.filename }}-*.deb

  cleanup:
    needs: [build-rustdesk-android, deploy]
    runs-on: ubuntu-latest
    continue-on-error: true
    if: always()
    steps:
      - name: Delete secrets artifact
        uses: geekyeggo/delete-artifact@v5
        with:
          name: encrypted-secrets-zip
